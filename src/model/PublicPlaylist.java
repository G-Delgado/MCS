package model;
public class PublicPlaylist extends Playlist {
	// Me falta agregar la calificacion que va a tener, va a ser un double. 
	public PublicPlaylist(String name, Duration duration) {
		super(name, duration);
	}

}